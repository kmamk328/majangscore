import React from 'react';
import { View, Text, SafeAreaView } from 'react-native';

const RuleScreen = () => {
  // ルール設定に関するロジックはここに実装

  return (
    <SafeAreaView>
      <Text>るーる設定</Text>
      {/* ルール設定の表示 */}
    </SafeAreaView>
  );
};

export default RuleScreen;
